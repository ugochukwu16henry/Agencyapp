export default function SubmitPage() {
  return (
    <main className="mx-auto max-w-3xl space-y-4 px-4 py-10 sm:px-6">
      <h1 className="text-3xl font-bold text-slate-900">Partner Mobile Intake</h1>
      <p className="text-sm text-slate-600">
        Use API route <code>POST /api/properties/submit</code> to create pending
        listings with private documents.
      </p>
    </main>
  );
}
